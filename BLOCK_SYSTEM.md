# ระบบ Block และ Unblock ผู้ใช้งาน

## คุณสมบัติ

ระบบนี้ช่วยให้แอดมินสามารถบล็อกและปลดบล็อกผู้ใช้งานได้ โดยกำหนดระยะเวลาการบล็อกได้ตั้งแต่ **3 วัน ถึง 30 วัน (1 เดือน)**

---

## การเปลี่ยนแปลงในระบบ

### 1. Database Schema (Prisma)

เพิ่มฟิลด์ใหม่ใน User model:

```prisma
model User {
  // ... ฟิลด์เดิม
  isBlocked         Boolean        @default(false)   // สถานะการบล็อก
  blockedReason     String?                          // เหตุผลที่ถูกบล็อก
  blockedAt         DateTime?                        // วันเวลาที่ถูกบล็อก
  blockedUntil      DateTime?                        // วันเวลาที่จะหมดอายุการบล็อก
  blockedBy         String?        @db.ObjectId      // ID ของแอดมินที่บล็อก
  // ...
}
```

เพิ่ม Action ใหม่ใน ActivityAction enum:
```prisma
enum ActivityAction {
  // ... action เดิม
  BLOCK
  UNBLOCK
}
```

**คำสั่งที่ใช้:**
```bash
npx prisma db push
npx prisma generate
```

---

### 2. API Endpoints

#### 2.1 Block User
**Endpoint:** `PUT /api/admin/users/[id]/block`

**Request Body:**
```json
{
  "duration": 7,  // จำนวนวัน (3-30)
  "reason": "เหตุผลในการบล็อก"
}
```

**Response:**
```json
{
  "message": "User blocked successfully for 7 days",
  "user": {
    "id": "...",
    "username": "...",
    "email": "...",
    "isBlocked": true,
    "blockedReason": "เหตุผลในการบล็อก",
    "blockedAt": "2025-01-24T10:00:00.000Z",
    "blockedUntil": "2025-01-31T10:00:00.000Z",
    "blockedBy": "admin_id"
  }
}
```

**Features:**
- ตรวจสอบว่าผู้ใช้ถูกบล็อกอยู่แล้วหรือไม่
- กำหนดระยะเวลาบล็อก 3-30 วัน
- บันทึก activity log
- ต้องมีสิทธิ์ ADMIN

---

#### 2.2 Unblock User
**Endpoint:** `PUT /api/admin/users/[id]/unblock`

**Response:**
```json
{
  "message": "User unblocked successfully",
  "user": {
    "id": "...",
    "username": "...",
    "email": "...",
    "isBlocked": false,
    "blockedReason": null,
    "blockedAt": null,
    "blockedUntil": null
  }
}
```

**Features:**
- ตรวจสอบว่าผู้ใช้ถูกบล็อกอยู่หรือไม่
- ล้างข้อมูลการบล็อกทั้งหมด
- บันทึก activity log
- ต้องมีสิทธิ์ ADMIN

---

### 3. Authentication Middleware

**ไฟล์:** `lib/auth.ts`

**การทำงาน:**
1. เมื่อผู้ใช้พยายาม login ระบบจะตรวจสอบว่า `isBlocked === true` หรือไม่
2. ถ้าถูกบล็อก:
   - ตรวจสอบว่า `blockedUntil` หมดอายุแล้วหรือยัง
   - ถ้าหมดอายุ → ปลดบล็อกอัตโนมัติและให้ login ได้
   - ถ้ายังไม่หมดอายุ → แสดง error message พร้อมเหตุผลและวันหมดอายุ

**Error Message:**
```
บัญชีของคุณถูกระงับการใช้งานจนถึง [วันที่]
เหตุผล: [เหตุผลที่ระบุ]
```

---

### 4. UI Components

#### 4.1 UserModalBlock
**ไฟล์:** `containers/user/UserModalBlock.tsx`

**Features:**
- ฟอร์มกำหนดระยะเวลา (3-30 วัน)
- Input สำหรับระบุเหตุผล (required)
- แสดงวันที่จะหมดอายุการบล็อก
- แสดงคำเตือนและสรุปข้อมูล
- Validation ระยะเวลา

**Props:**
```typescript
interface UserModalBlockProps {
  data: UserJoined;           // ข้อมูลผู้ใช้
  onSuccess?: () => void;     // Callback เมื่อบล็อกสำเร็จ
  triggerClassName?: string;   // Custom class สำหรับปุ่ม
}
```

---

#### 4.2 UserModalUnblock
**ไฟล์:** `containers/user/UserModalUnblock.tsx`

**Features:**
- แสดงข้อมูลการบล็อกปัจจุบัน (วันที่บล็อก, วันหมดอายุ, เหตุผล)
- แสดงจำนวนวันที่เหลือ
- ยืนยันการปลดบล็อก

**Props:**
```typescript
interface UserModalUnblockProps {
  data: UserJoined;           // ข้อมูลผู้ใช้
  onSuccess?: () => void;     // Callback เมื่อปลดบล็อกสำเร็จ
  triggerClassName?: string;   // Custom class สำหรับปุ่ม
}
```

---

#### 4.3 Admin Users Page
**ไฟล์:** `app/admin/users/page.tsx`

**การเปลี่ยนแปลง:**
1. เพิ่มคอลัมน์ "สถานะ" ในตาราง
   - แสดง badge "ถูกบล็อก" (สีแดง) ถ้าถูกบล็อก
   - แสดง badge "ปกติ" (สีเขียว) ถ้าปกติ
   - แสดงวันหมดอายุการบล็อก

2. เพิ่มปุ่ม Block/Unblock
   - แสดงปุ่ม Block (สีส้ม) ถ้าผู้ใช้ไม่ถูกบล็อก
   - แสดงปุ่ม Unblock (สีเขียว) ถ้าผู้ใช้ถูกบล็อกอยู่

---

#### 4.4 UserModalView
**ไฟล์:** `containers/user/UserModalView.tsx`

**การเปลี่ยนแปลง:**
- เพิ่มส่วนแสดงข้อมูลการบล็อก (ถ้าถูกบล็อก)
  - วันที่บล็อก
  - วันหมดอายุ
  - เหตุผล
- อัปเดตสถานะบัญชีให้แสดง "ถูกบล็อก" ด้วย

---

## วิธีการใช้งาน

### สำหรับแอดมิน

#### 1. บล็อกผู้ใช้
1. เข้าไปที่หน้า Admin > จัดการผู้ใช้
2. คลิกปุ่ม 🚫 (Block) ที่ผู้ใช้ที่ต้องการบล็อก
3. กำหนดระยะเวลา (3-30 วัน)
4. ระบุเหตุผล
5. ยืนยันการบล็อก

#### 2. ปลดบล็อกผู้ใช้
1. เข้าไปที่หน้า Admin > จัดการผู้ใช้
2. คลิกปุ่ม 🔓 (Unblock) ที่ผู้ใช้ที่ถูกบล็อก
3. ตรวจสอบข้อมูลการบล็อก
4. ยืนยันการปลดบล็อก

#### 3. ดูข้อมูลการบล็อก
1. คลิกปุ่ม 👁️ (ดูรายละเอียด)
2. ระบบจะแสดงส่วน "ผู้ใช้ถูกบล็อก" ด้วยข้อมูลครบถ้วน

---

### สำหรับผู้ใช้ที่ถูกบล็อก

เมื่อพยายาม login:
```
❌ บัญชีของคุณถูกระงับการใช้งานจนถึง 31 มกราคม 2568
เหตุผล: ละเมิดเงื่อนไขการใช้งาน
```

---

## Activity Logging

ระบบจะบันทึก activity log ทุกครั้งที่มีการ block/unblock:

### Block Activity
```json
{
  "action": "BLOCK",
  "model": "User",
  "modelId": "user_id",
  "oldData": "{\"isBlocked\":false}",
  "newData": "{\"isBlocked\":true,\"blockedReason\":\"...\",\"blockedUntil\":\"...\",\"duration\":7}",
  "description": "Admin blocked user [username] for 7 days. Reason: [...]",
  "ipAddress": "...",
  "userAgent": "..."
}
```

### Unblock Activity
```json
{
  "action": "UNBLOCK",
  "model": "User",
  "modelId": "user_id",
  "oldData": "{\"isBlocked\":true,\"blockedReason\":\"...\",\"blockedUntil\":\"...\"}",
  "newData": "{\"isBlocked\":false}",
  "description": "Admin unblocked user [username]",
  "ipAddress": "...",
  "userAgent": "..."
}
```

---

## การทดสอบ

### 1. ทดสอบการบล็อก
```bash
# เริ่มเซิร์ฟเวอร์
npm run dev

# ทดสอบ API
curl -X PUT http://localhost:3000/api/admin/users/[user_id]/block \
  -H "Content-Type: application/json" \
  -d '{"duration": 7, "reason": "Test block"}'
```

### 2. ทดสอบการปลดบล็อก
```bash
curl -X PUT http://localhost:3000/api/admin/users/[user_id]/unblock
```

### 3. ทดสอบ UI
1. รัน `npm run dev`
2. Login เป็น Admin
3. ไปที่ `/admin/users`
4. ทดสอบบล็อก/ปลดบล็อกผู้ใช้
5. ทดสอบ login ด้วยบัญชีที่ถูกบล็อก

---

## Error Handling

### API Errors
- `401`: ไม่มีสิทธิ์ (ไม่ใช่ Admin)
- `400`: ข้อมูลไม่ถูกต้อง (duration ไม่อยู่ในช่วง 3-30)
- `404`: ไม่พบผู้ใช้
- `500`: เกิดข้อผิดพลาดในระบบ

### Client-side Validation
- ตรวจสอบ duration 3-30 วัน
- ตรวจสอบเหตุผลไม่ว่าง
- แสดง toast notification เมื่อสำเร็จหรือล้มเหลว

---

## Auto-unblock

ระบบจะปลดบล็อกอัตโนมัติเมื่อ:
1. ผู้ใช้ที่ถูกบล็อกพยายาม login
2. เวลาปัจจุบัน >= `blockedUntil`
3. ระบบจะ update `isBlocked = false` และล้างข้อมูลการบล็อกทั้งหมด
4. อนุญาตให้ login ได้ทันที

---

## สิ่งที่ต้องทำต่อหลังจากสร้างระบบ

1. ✅ Update Prisma schema
2. ✅ Push schema to database
3. ✅ สร้าง API endpoints
4. ✅ อัปเดต authentication middleware
5. ✅ สร้าง UI components
6. ✅ อัปเดตหน้า Admin
7. ⏳ รันเซิร์ฟเวอร์และทดสอบระบบ
8. ⏳ ตรวจสอบ TypeScript errors (จะหายเมื่อรีสตาร์ทเซิร์ฟเวอร์)

---

## คำแนะนำเพิ่มเติม

1. **รีสตาร์ทเซิร์ฟเวอร์:** TypeScript จะโหลด Prisma types ใหม่และ errors จะหาย
2. **ตรวจสอบ Activity Logs:** ดูประวัติการ block/unblock ที่ ActivityLog
3. **Customize Duration:** ถ้าต้องการเปลี่ยนช่วงเวลา แก้ไขได้ที่ API validation
4. **Email Notification:** อาจเพิ่มการส่งอีเมลแจ้งเตือนเมื่อถูกบล็อก/ปลดบล็อก

---

## ไฟล์ที่เกี่ยวข้อง

- `prisma/schema.prisma` - Database schema
- `lib/auth.ts` - Authentication logic with block checking
- `app/api/admin/users/[id]/block/route.ts` - Block API
- `app/api/admin/users/[id]/unblock/route.ts` - Unblock API
- `containers/user/UserModalBlock.tsx` - Block modal component
- `containers/user/UserModalUnblock.tsx` - Unblock modal component
- `containers/user/UserModalView.tsx` - View user with block info
- `app/admin/users/page.tsx` - Admin users page
- `types/index.ts` - TypeScript types (auto-updated from Prisma)

---

**สร้างโดย:** Claude Code
**วันที่:** 24 ตุลาคม 2568
