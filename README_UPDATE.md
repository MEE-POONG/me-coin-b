# 🎉 MeCoins - System Update Summary

## ✨ สรุปการอัปเดตระบบ

โปรเจค MeCoins ได้รับการอัปเดตครั้งใหญ่! เพิ่มฟีเจอร์ใหม่มากมายเพื่อให้เป็นระบบที่สมบูรณ์แบบยิ่งขึ้น

---

## 📊 Models ทั้งหมดในระบบ

### 1. 👤 User
ข้อมูลผู้ใช้ระบบ
- ข้อมูลพื้นฐาน: email, username, accountNumber, avatar
- Role: ADMIN, NORMAL, PREMIUM
- Discord integration (optional)
- Relations: wallet, deposits, withdrawals, purchases, gifts, transfers

### 2. 💰 Wallet
กระเป๋าเงินของผู้ใช้ (แยกจาก User)
- balance: ยอดเงินคงเหลือ
- เชื่อมโยงกับ User แบบ 1:1
- เก็บประวัติ transactions ทั้งหมด

### 3. 📊 Transaction
ประวัติธุรกรรมทั้งหมด
- Types: DEPOSIT, WITHDRAWAL, PURCHASE, GIFT, TRANSFER
- Status: PENDING, COMPLETED, FAILED, CANCELLED
- เชื่อมโยงกับ: user, wallet, deposit, withdrawal, purchase, gift, transfer

### 4. 💵 Deposit
คำขอฝากเงิน (รอ Admin อนุมัติ)
- amount: จำนวนเงิน
- slipImage: รูปสลิปการโอน
- rate: อัตราแลกเปลี่ยน
- status: PENDING, APPROVED, REJECTED
- comment: หมายเหตุจาก Admin

### 5. 💸 Withdrawal
คำขอถอนเงิน (รอ Admin อนุมัติ)
- amount: จำนวนเงิน
- status: PENDING, APPROVED, REJECTED
- comment: หมายเหตุจาก Admin

### 6. 🛍️ Item
ไอเทมในร้านค้า
- name, description, price, imageUrl
- category: หมวดหมู่
- rarity: COMMON, RARE, EPIC, LEGENDARY

### 7. 🎒 OwnedItem
ไอเทมที่ผู้ใช้เป็นเจ้าของ
- userId, itemId
- isGifted: บอกว่าได้มาจากของขวัญหรือไม่
- เชื่อมโยงกับ purchases และ gifts

### 8. 🛒 Purchase
ประวัติการซื้อไอเทม
- userId, ownedItemId
- เก็บข้อมูลการซื้อ
- เชื่อมโยงกับ Transaction

### 9. 🎁 Gift
ประวัติการให้ของขวัญ
- senderId, recipientId, ownedItemId
- โอนความเป็นเจ้าของไอเทม
- เชื่อมโยงกับ Transaction

### 10. 🔄 Transfer
ประวัติการโอนเงิน
- senderId, receiverId, amount
- comment: หมายเหตุ
- status: PENDING, COMPLETED, FAILED, CANCELLED

### 11. 💎 DepositRate
อัตราแลกเปลี่ยนการฝาก
- name, rate
- startDate, endDate: ช่วงเวลาที่ใช้งาน
- isActive: เปิด/ปิดใช้งาน

---

## 🔌 API Endpoints ทั้งหมด

### Authentication
- `POST /api/auth/signin` - เข้าสู่ระบบ
- `POST /api/auth/signout` - ออกจากระบบ
- `GET /api/auth/session` - ดู session

### Users
- `GET /api/users/me` - ดูข้อมูลตัวเอง
- `GET /api/admin/users` - ดูผู้ใช้ทั้งหมด (Admin)

### Wallet
- `GET /api/wallet` - ดู wallet ของตัวเอง
- `POST /api/wallet` - สร้าง wallet

### Transactions
- `GET /api/transactions` - ดูประวัติธุรกรรม (แบ่งหน้า + filter)

### Deposits
- `GET /api/deposits` - ดูรายการฝาก
- `POST /api/deposits` - สร้างคำขอฝาก
- `PATCH /api/deposits/:id` - อนุมัติ/ปฏิเสธ (Admin)

### Withdrawals
- `GET /api/withdrawals` - ดูรายการถอน
- `POST /api/withdrawals` - สร้างคำขอถอน
- `PATCH /api/withdrawals/:id` - อนุมัติ/ปฏิเสธ (Admin)

### Items
- `GET /api/items` - ดูรายการไอเทม (แบ่งหน้า + filter)
- `POST /api/items` - สร้างไอเทม (Admin)
- `GET /api/items/:id` - ดูไอเทมเดี่ยว
- `PUT /api/items/:id` - แก้ไขไอเทม (Admin)
- `DELETE /api/items/:id` - ลบไอเทม (Admin)

### Owned Items
- `GET /api/owned-items` - ดูไอเทมของตัวเอง

### Purchases
- `GET /api/purchases` - ดูประวัติการซื้อ
- `POST /api/purchases` - ซื้อไอเทม

### Gifts
- `GET /api/gifts` - ดูประวัติของขวัญ (filter: sent/received)
- `POST /api/gifts` - ส่งของขวัญ

### Transfers
- `GET /api/transfers` - ดูประวัติโอนเงิน (filter: sent/received)
- `POST /api/transfers` - โอนเงิน

### Deposit Rates
- `GET /api/deposit-rates` - ดูอัตราแลกเปลี่ยน
- `POST /api/deposit-rates` - สร้างอัตรา (Admin)
- `PUT /api/deposit-rates/:id` - แก้ไขอัตรา (Admin)
- `DELETE /api/deposit-rates/:id` - ลบอัตรา (Admin)

### Admin Stats
- `GET /api/admin/stats` - ดูสถิติภาพรวม (Admin)

---

## 🎯 ฟีเจอร์หลัก

### สำหรับผู้ใช้ (User/Premium)
1. ✅ ดูข้อมูลส่วนตัวและยอดเงินใน Wallet
2. ✅ สร้างคำขอฝากเงิน (พร้อมอัพโหลดสลิป)
3. ✅ สร้างคำขอถอนเงิน
4. ✅ ดูรายการไอเทมในร้านค้า
5. ✅ ซื้อไอเทมด้วยเงินใน Wallet
6. ✅ ดูไอเทมที่เป็นเจ้าของ
7. ✅ ส่งไอเทมเป็นของขวัญให้ผู้อื่น
8. ✅ โอนเงินให้ผู้ใช้คนอื่น
9. ✅ ดูประวัติธุรกรรมทั้งหมด (แบ่งหน้า)
10. ✅ ดูประวัติของขวัญที่ส่งและได้รับ
11. ✅ ดูประวัติการโอนเงิน

### สำหรับแอดมิน (Admin)
1. ✅ อนุมัติ/ปฏิเสธคำขอฝากเงิน
2. ✅ อนุมัติ/ปฏิเสธคำขอถอนเงิน
3. ✅ จัดการไอเทม (สร้าง/แก้ไข/ลบ)
4. ✅ จัดการอัตราแลกเปลี่ยน
5. ✅ ดูรายการผู้ใช้ทั้งหมด
6. ✅ ดูสถิติภาพรวมของระบบ
7. ✅ ดูประวัติธุรกรรมทั้งหมด
8. ✅ ดูรายการฝาก/ถอนทั้งหมด

---

## 🔐 ระบบรักษาความปลอดภัย

1. **Authentication**: NextAuth.js with JWT
2. **Password Hashing**: bcrypt
3. **Role-based Access Control**: ADMIN, NORMAL, PREMIUM
4. **Protected Routes**: Middleware ตรวจสอบ session
5. **Database Transactions**: ใช้ Prisma transactions สำหรับการโอนเงิน
6. **Balance Validation**: ตรวจสอบยอดเงินก่อนทำธุรกรรม
7. **Ownership Validation**: ตรวจสอบความเป็นเจ้าของก่อนโอนไอเทม

---

## 📊 Database Relations

```
User (1) ─── (1) Wallet
  │
  ├── (1:N) Transactions
  ├── (1:N) Deposits
  ├── (1:N) Withdrawals
  ├── (1:N) Purchases
  ├── (1:N) OwnedItems
  ├── (1:N) SentGifts
  ├── (1:N) ReceivedGifts
  ├── (1:N) SentTransfers
  └── (1:N) ReceivedTransfers

Item (1) ─── (N) OwnedItem (1) ─── (1) Purchase
                   │
                   └── (1) Gift

Transaction ─── Deposit/Withdrawal/Purchase/Gift/Transfer
```

---

## 🚀 วิธีเริ่มใช้งาน

### 1. ติดตั้ง Dependencies
```bash
npm install
```

### 2. ตั้งค่า Environment
```bash
# Copy .env.example
cp .env.example .env

# แก้ไข .env
DATABASE_URL="postgresql://..."
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret"
```

### 3. Setup Database
```bash
# Generate Prisma Client
npx prisma generate

# Push schema
npx prisma db push

# Seed data
npx prisma db seed
```

### 4. รัน Development Server
```bash
npm run dev
```

### 5. เปิด Browser
```
http://localhost:3000
```

---

## 👥 บัญชีทดสอบ

### Admin
- Email: `admin@example.com`
- Password: `admin123`
- Wallet: 10,000 บาท

### Normal User
- Email: `user@example.com`
- Password: `user123`
- Wallet: 5,000 บาท

### Premium User
- Email: `premium@example.com`
- Password: `premium123`
- Wallet: 8,000 บาท

---

## 📚 เอกสารเพิ่มเติม

- [API Documentation](./API_DOCUMENTATION.md) - เอกสาร API แบบละเอียด
- [Migration Guide](./MIGRATION_GUIDE.md) - คู่มือการ migrate จาก version เก่า
- [Setup Guide](./SETUP.md) - คู่มือการติดตั้ง

---

## 🔄 Flow การทำงาน

### การฝากเงิน (Deposit)
1. User สร้างคำขอฝาก + อัพโหลดสลิป
2. Status: PENDING
3. Admin ดูรายการฝากที่รอการอนุมัติ
4. Admin อนุมัติ → เงินเข้า Wallet + สร้าง Transaction
5. Admin ปฏิเสธ → สร้าง Transaction แบบ FAILED

### การถอนเงิน (Withdrawal)
1. User สร้างคำขอถอน
2. ระบบเช็คว่ามียอดเงินพอหรือไม่
3. Status: PENDING
4. Admin ดูรายการถอนที่รอการอนุมัติ
5. Admin อนุมัติ → หักเงินจาก Wallet + สร้าง Transaction
6. Admin ปฏิเสธ → สร้าง Transaction แบบ FAILED

### การซื้อไอเทม (Purchase)
1. User เลือกไอเทมที่ต้องการซื้อ
2. ระบบเช็คว่ามียอดเงินพอหรือไม่
3. หักเงินจาก Wallet
4. สร้าง OwnedItem ให้ User
5. สร้าง Purchase record
6. สร้าง Transaction

### การให้ของขวัญ (Gift)
1. User เลือกไอเทมที่เป็นเจ้าของ
2. เลือกผู้รับของขวัญ
3. ระบบเช็คว่าเป็นเจ้าของจริงๆ
4. โอนความเป็นเจ้าของไอเทมให้ผู้รับ
5. สร้าง Gift record
6. สร้าง Transaction

### การโอนเงิน (Transfer)
1. User ระบุผู้รับและจำนวนเงิน
2. ระบบเช็คว่ามียอดเงินพอหรือไม่
3. หักเงินจาก Wallet ผู้ส่ง
4. เพิ่มเงินให้ Wallet ผู้รับ
5. สร้าง Transfer record
6. สร้าง Transaction 2 รายการ (ผู้ส่ง + ผู้รับ)

---

## 🎨 Tech Stack

- **Frontend**: Next.js 14, React, TypeScript
- **Styling**: Tailwind CSS
- **Authentication**: NextAuth.js
- **Database**: PostgreSQL
- **ORM**: Prisma
- **Password**: bcrypt

---

## 📝 TODO (ฟีเจอร์ในอนาคต)

- [ ] อัพโหลดรูปภาพสลิป/ไอเทม (Cloud Storage)
- [ ] ระบบแจ้งเตือน (Notifications)
- [ ] Discord Integration (OAuth)
- [ ] Premium Features
- [ ] ระบบ Referral
- [ ] ระบบ Daily Rewards
- [ ] ระบบ Achievement/Badges
- [ ] Export ประวัติเป็น PDF
- [ ] Dashboard Charts/Graphs

---

## 🤝 Contributing

ยินดีรับ contribution ทุกรูปแบบ! กรุณา fork และส่ง pull request

---

## 📄 License

MIT License

---

**สร้างด้วย ❤️ โดย AI Assistant**

